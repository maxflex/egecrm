// Generated by CoffeeScript 1.9.3
angular.module("Promo", []).controller("MainCtrl", function($scope) {
  $scope.sklName = function(person, padej) {
    if (person === void 0) {
      return false;
    }
    person = {
      first: person.first_name,
      last: person.last_name,
      middle: person.middle_name
    };
    person = petrovich(person, padej);
    return person.last + ' ' + person.first;
  };
  $scope.students_count = 0;
  return angular.element(document).ready(function() {
    set_scope("Promo");
    $('.counter').FlipClock($scope.students_count, {
      autoStart: false,
      clockFace: 'Counter'
    });
    return $('.flip.play').first().hide();
  });
});
